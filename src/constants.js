// localStorage keys
export const STORAGE_SETTINGS = 'mkdn-settings';
export const STORAGE_CUSTOM_THEME = 'mkdn-custom-theme';
export const STORAGE_PLUGINS = 'mkdn-plugins';
export const STORAGE_PLUGINS_ENABLED = 'mkdn-plugins-enabled';
export const STORAGE_COLLAB_NAME = 'mkdn-collab-name';
export const STORAGE_CMD_RECENT = 'mkdn-cmd-recent';
export const STORAGE_CUSTOM_TEMPLATES = 'mkdn-custom-templates';

// sessionStorage keys
export const SESSION_COLLAB = 'mkdn-collab-session';
export const SESSION_GITHUB_TOKEN = 'mkdn-github-token';

// IndexedDB database names
export const DB_HISTORY = 'mkdn-history';
export const DB_HANDLES = 'mkdn-handles';
export const DB_VECTORS = 'mkdn-vectors';

// Session/storage keys (non-prefixed pattern)
export const STORAGE_SESSION = 'mkdn-session';

// Plugin storage prefix (used dynamically as `mkdn-plugin-${id}-${key}`)
export const PLUGIN_STORAGE_PREFIX = 'mkdn-plugin-';

// Recent files (file switcher)
export const STORAGE_RECENT_FILES = 'mkdn-recent-files';

// PartyKit room prefix
export const PARTYKIT_ROOM_PREFIX = 'mkdn-';

// DOM element IDs
export const CUSTOM_THEME_STYLE_ID = 'mkdn-custom-theme';
